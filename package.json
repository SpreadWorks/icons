{
  "name": "company-icons-workspace",
  "private": true,
  "packageManager": "pnpm@8.15.9",
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "scripts": {
    "build": "pnpm --filter @company/icons build && pnpm --filter @company/icon-generator build",
    "test": "pnpm --filter @company/icons test && pnpm --filter @company/icon-generator test",
    "verify": "pnpm build && pnpm test && node scripts/verify-public-package.mjs && node scripts/test-consumer.mjs"
  }
}
